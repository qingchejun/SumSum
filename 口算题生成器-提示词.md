# 提示词：小学口算题生成与打印网站

请帮我做一个**纯前端、无后端、无需登录**的小学口算练习题生成网站。目标用户是家长：在电脑或手机上设置参数 → 生成题目 → 直接用浏览器打印成 A4 练习纸给孩子做。

## 技术要求

- 单页应用，HTML + CSS + 原生 JS（或 Vite + React，二选一，优先简单）。不依赖后端和数据库。
- 所有设置保存在 localStorage，刷新后保留；同时把设置编码进 URL 参数，方便收藏"每日固定题型"的链接。
- 能直接部署到 Vercel / GitHub Pages / Cloudflare Pages 这类静态托管。
- 代码结构清晰：题目生成逻辑（generator）、页面 UI、打印样式三部分分开，方便以后加题型。
- 移动端可用（设置面板在手机上要能正常操作），但打印是核心场景，优先保证桌面端和打印效果。

## 功能一：运算模式（单选）

1. 加法
2. 减法
3. 加减法混合（随机出加或减）
4. 乘法
5. 除法
6. 乘除法混合
7. 四则运算（每题 2～3 步，可选是否带括号）

## 功能二：参数自定义

- **数值范围**：起始数字（最小值）和最大值。加减法按"结果范围"控制，例如"20 以内"指参与数和结果都不超过 20。
- **题目数量**：默认 20，可选 10～100。
- **每行题数 / 列数**：默认 3 列，可选 2～4 列。
- **减法不出负数**（默认开）；**除法必须整除**（默认开），关掉后显示"…余…"。
- **加减进位 / 退位控制**：可选"不进位"、"只出进位"、"随机"（对低年级很重要）。
- **乘除法数值范围**：乘数默认 1～9（九九乘法表范围），可扩大。
- **题目不重复**（默认开）。
- **填空位置**：默认答案在末尾 `12 + 5 = ___`；可选"随机挖空"，如 `12 + ___ = 17` 或 `___ − 5 = 8`。
- **答案页**：可选"打印时附答案页"（答案单独一页，方便家长批改）。
- **纸张**：默认 A4 纵向，可选 A4 横向。
- 一键"重新生成"（保持参数不变换一批题），以及若干**预设按钮**：10 以内加减、20 以内加减（进位/退位）、100 以内加减、乘法口诀、四则运算入门。

## 功能三：打印

- 点击"打印"直接调用 `window.print()`，不需要跳转或下载。
- 用 `@media print` 单独写打印样式：隐藏设置面板和按钮，只输出练习纸。
- `@page { size: A4; margin: 15mm }`，题目排版**保证不跨页截断**（每题 `break-inside: avoid`），题目多时自动分页，每页都保留页眉。
- 打印色彩安全：纯黑字、无背景色、无阴影。

## 排版要求（练习纸）

参考截图的样式（描述如下，请按此还原）：

- 页眉一行：左侧标题（如"20 以内加减法练习"），右侧留空写：`姓名：______  日期：____月____日  用时：____分  得分：____`。
- 题目区：网格排列，默认 3 列，题目按**先横后竖**编号（1、2、3 在第一行）。
- 每题格式：`①  12 + 5 = ______`，题号小一号，算式用大号等宽或清晰无衬线字体（≥ 18pt），等号后留足够手写空间，行间距要大，适合一年级孩子手写。
- 每页底部小字页码：`第 1 页 / 共 2 页`。
- 答案页与题目页排版一致，但答案直接写在等号后并用不同标题标明"答案"。

> （这里粘贴或描述你截图里的具体细节，例如列数、是否有边框、题号样式、字号大小等。）

## 交互细节

- 生成后页面上即时预览，所见即所得，预览就是打印效果。
- 参数改变自动重新生成（或提供明显的"生成"按钮），不要让用户找不到入口。
- 界面中文，简洁，不要花哨；控制面板放左侧或顶部，练习纸预览在主区域。

## 交付

- 请给出完整可运行的代码，并说明本地打开方式和部署方式。
- 生成逻辑请写清楚，并在代码注释里说明每种模式的取数规则，方便我以后自己改。
